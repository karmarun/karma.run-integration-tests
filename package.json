{
  "private": true,
  "scripts": {
    "watch": "run-p watch:*",
    "watch:sdk": "cd sdk && yarn watch",
    "watch:tests": "cd tests && yarn watch",
    "test": "cd tests && yarn test --verbose"
  },
  "workspaces": [
    "sdk",
    "tests"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.2",
    "tslint": "^5.9.1",
    "typescript": "^2.8.3"
  }
}

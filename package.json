{
  "private": true,
  "scripts": {
    "watch": "run-p watch:*",
    "watch:sdk": "cd sdk && yarn watch",
    "watch:test": "cd tests && yarn watch",
    "test": "cd tests && yarn test",
    "test-nyan": "cd tests && yarn test-nyan"
  },
  "workspaces": [
    "sdk",
    "tests"
  ],
  "devDependencies": {
    "npm-run-all": "^4.1.2",
    "tslint": "^5.9.1",
    "typescript": "^2.8.3"
  }
}
